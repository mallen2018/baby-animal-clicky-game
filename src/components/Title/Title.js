import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Baby Animal Clicky Game!</h1>
        <p>Test your memory! Click on an image to earn points, but don't click on one image more than once!</p>
    </div>
);

export default Title;



